user design code is fetched onto rabbitmq
rabbitmq contents and compiled and the output is evaluated based on resultant text
evaluation result questionsolved(changed) and testcasesolved(changed)
InputTextId, OutputTextId from db for aftercodeexec evaluation
One exec queue and one resultant queue
exec queue is to push the user code with the db content based on InputTextId, OutputTextId and other details
output is brought onto resultant queue and based on matching between OutputTextId db content TestCaseScore(changes) or QuestionScore(changes)
and db is manipulated little (ie user solved this question/vice versa)
user will execution ie after entering the answer code, we keep hitting GET /result/{jobId}(ex)
single or two queue structure
